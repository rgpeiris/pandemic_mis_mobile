import React from "react";
import { Box, Typography } from "@mui/material";

const CovidVaccineAddBeneficiariesWeb = () => {
  return (
    <Box
      sx={{
        height: "100%",
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <Typography align="center" variant="h5">
        Covid Vaccine Add Beneficiaries Web
      </Typography>
    </Box>
  );
};

export default CovidVaccineAddBeneficiariesWeb;
