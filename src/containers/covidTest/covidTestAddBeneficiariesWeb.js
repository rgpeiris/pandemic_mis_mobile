import React from "react";
import { Box, Typography } from "@mui/material";

const CovidTestAddBeneficiariesWeb = () => {
  return (
    <Box
      sx={{
        height: "100%",
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <Typography align="center" variant="h5">
        Covid Test Add Beneficiaries Web
      </Typography>
    </Box>
  );
};

export default CovidTestAddBeneficiariesWeb;
