import React from "react";
import { Box, Typography } from "@mui/material";

const AddTestsBeneficiaryWeb = () => {
  return (
    <Box
      sx={{
        height: "100%",
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <Typography align="center" variant="h5">
        Add Beneficiary Web
      </Typography>
    </Box>
  );
};

export default AddTestsBeneficiaryWeb;
