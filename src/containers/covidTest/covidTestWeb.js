import React from "react";
import { Box, Typography } from "@mui/material";

const CovidTestWeb = () => {
  return (
    <Box
      sx={{
        height: "100%",
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <Typography align="center" variant="h5">
        COVID Viral Tests Web
      </Typography>
    </Box>
  );
};

export default CovidTestWeb;
